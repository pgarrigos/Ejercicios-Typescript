function mostrarNumeros(numeros: number[]) {
  for (let index = 0; index < numeros.length; index++) {
    const element = numeros[index];
    console.log(element);
  }
}
mostrarNumeros([20, 30, 40]);
