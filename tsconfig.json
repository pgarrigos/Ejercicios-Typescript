{
  "compilerOptions": {
    "target": "es6", // Genera JavaScript ES6
    "module": "commonjs", // Sistema de módulos compatible con Node.js
    "strict": true, // Activa todas las comprobaciones estrictas
    "esModuleInterop": true, // Permite importar módulos CommonJS como si fueran ESModules
    "forceConsistentCasingInFileNames": true, // Evita errores por nombres de archivos mal escritos (mayúsculas/minúsculas)
    "skipLibCheck": true // No verifica definiciones de tipos externas (más rápido)
  },
  "include": ["*.ts"], // Incluye todos los .ts de esta carpeta
  "exclude": ["node_modules"] // Ignora la carpeta node_modules
}
